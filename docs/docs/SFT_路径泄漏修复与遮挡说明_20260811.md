# SFT 路径泄漏修复与数据遮挡说明（2026-08-11）

## 1. 背景：泄漏发现与影响链

R2E-Gym SFT Trajectories 源数据全量携带 SWE-bench 容器工作区根 `/testbed` 的
绝对路径引用。污染沿 normalize -> tokenize -> sft_dataset -> train.jsonl
管线原样进入训练输入，并落在 **mask=1 的 assistant 段**（CE 学习目标），
直接强化"工具调用参数必须写 /testbed 绝对路径"：

```
SFT 教授绝对路径参数（mask=1, 被强化）
  -> RL 环境工作区根是 /workspace（Mac §8 server）
  -> 模型沿用 /testbed/... -> 工具拒绝（"path must be relative" / 工作区外）
  -> patch=0 -> reward 恒 uniform -> advantages=0 -> 零梯度（v13 实证）
```

**污染前的 token 级证据**（600 条记录逐 token 匹配落库数组）:

| 位置 | 命中 | 含义 |
|---|---|---|
| `response_ids` mask=1 | 9,268 | **CE 学习目标，直接强化** |
| `response_ids` mask=0 | 0 | — |
| `prompt_ids`（context） | 128,050 | 可见不计 loss |

600/600 条记录（100%）在 mask=1 位置含 `/testbed`；tool_call arguments 共
29,574 处、tool 输出 `/testbed:1-99` 标注 2,219 处、task 描述全覆盖。

## 2. 修复：adapter 层路径 rebase

`src/sweforge/data/sft/adapters/r2egym.py` 新增 `_rebase_testbed`，
在 **normalize 源头**清洗（产物即 SFT/RL 共用 canonical 数据，一处修复全链路生效）：

```python
def _rebase_testbed(text: str) -> str:
    """SWE-bench 容器工作区根 /testbed -> RL 环境根 /workspace。"""
    return text.replace("/testbed", "/workspace")
```

**选择 /workspace 而非删除前缀成相对路径的理由**：
- 全局替换语义保持（`"in the /testbed directory"` -> `"in the /workspace directory"` 通顺）
- bash 输出 / traceback 中的路径替换后仍自洽
- RL 环境 Mac §8 server 支持 `/workspace` 前缀归一化（`_relpath_or_none`, 5/5 验证）
- 模型即使生成 `/workspace/foo.py` 绝对路径也能被环境接受

**作用点**（normalize 主流程两处，覆盖全部消息文本）：
1. `task_text = _rebase_testbed(messages[1].get("content") or "")` — 任务描述
2. 消息循环 `content = _rebase_testbed(m.get("content") or "")` — assistant 调用
   参数（view_file/str_replace/search path、bash cmd）、tool 输出、注释文本

**回归测试**（tests/test_r2egym_adapter.py, 36 passed）：
- `test_rebase_testbed_cleans_all_roles` — 全消息无 testbed 残留、含路径 action
  显式变为 /workspace、validate_trajectory 通过
- `test_rebase_testbed_task_id_stable_without_testbed`
- `test_parse_view_dir_listing` — 期望更新为 /workspace（dir listing 输出同被清洗）

## 3. 数据管线重跑（产物与统计）

| 步骤 | 命令 | 产物 | /testbed 残留 |
|---|---|---|---|
| normalize | `python -m sweforge.data.sft.normalize` | `data/r2egym/normalized/trajectories.jsonl` | 0（3061 条全量） |
| tokenize | `python -m sweforge.data.sft.tokenize` | `data/r2egym/sft_dataset.jsonl` | mask=1 段 0 次 |
| split | sha256(`sweforge-sft-v1:task_id`)%100<5 | `experiments/sft_qwen3_4b_lora_r16_e1_v2_20260811/data/{train,holdout}.jsonl` | train 2918 / holdout 143 |

token 级验证（全量 3061 条，`/testbed` 分词 `['/test'(12697), 'bed'(2721)]`
逐 token 匹配 + 前导 `/` 上下文判定）：

| 指标 | 清洗前 | 清洗后 |
|---|---|---|
| mask=1 段真实 `/testbed` 路径 token | 9,268（600 条） | **0**（3061 条） |
| prompt context `/testbed` | 128,050 | **0** |
| mask=1 段 `/workspace` token | 0 | 47,419 |
| prompt context `/workspace` | 6 | 321,806 |

**残留澄清**：mask=1 段仍含 1,045 个 `/test` token——全部来自代码内容中的
`testbed_dir` 变量名（`testbed_dir = os.path.dirname(os.path.abspath(__file__))`，
测试脚本自定位逻辑），**无前导斜杠、非路径引用**，不构成泄漏（模型学到的是
文件内容而非工作区路径格式）。RL pool.jsonl 的 42 处 `/testbed` 全部位于
`container_workspace` secret 字段，policy-visible TaskSpec 已剥离，无需处理。

## 4. 当前被训练数据的遮挡（mask）情况

### 4.1 统一 mask 语义（PROJECT_SPEC §0 P2 / §3）

```
system / user / tool 消息  ->  prompt_ids（context 可见, 不计 loss）   mask 无
assistant 消息             ->  response_ids, response_mask 等长       mask=1
assistant 段开头 3 token    ->  assistant_prefix_mask=3               mask=0
    （<|im_start|>assistant\n 模板注入的 generation prompt, 非模型该学的文本）
```

- `response_ids` 由全部 assistant 段按序拼接，`response_mask` 与之逐 token 对齐
- tool 消息（观察输出）进 `prompt_ids`：模型能看到，但不产生梯度
- 加载路径（dataset.py `loss_mask_from_trajectory`）直接取落库 `response_mask`
  切片进 CE loss——**存储即训练，无任何遮挡/重算**

### 4.2 实例样本：r2egym-sft-00000（task_id r2egym-9fe0daa48f）

```
prompt_ids   = 2005 token（system + user 任务 + 5 条 tool 观察, 全 context）
response_ids = 1493 token（8 条 assistant 段拼接, 掩码段结构:）
```

response_mask 压缩段（`[mask, 长度]` 交替，8 段）：

```
[0,3] [1,59]  [0,3] [1,54]  [0,3] [1,295]  [0,3] [1,37]
[0,3] [1,204] [0,3] [1,506] [0,3] [1,38]   [0,3] [1,276]
mask=1 合计 1469/1493 = 98.4%
```

每条 assistant 段结构：`[0,3]` 前缀（模板注入 `<|im_start|>assistant\n`，
不计 loss）+ `[1,N]` 主体（模型生成的注释 + `<tool_call>` JSON 参数，**全部
是 CE 学习目标**）。例（清洗后第一条 assistant 调用段内实际 token 文本）：

```
mask=1 段内含:
<tool_call>
{"name": "view_file", "arguments": {"path": "/workspace/scrapy/utils/datatypes.py", "start_line": null, "end_line": null}}
```

即：**模型被训练生成 `/workspace/...` 绝对路径参数**（RL 环境兼容）。
清洗前同位置是 `/testbed/scrapy/utils/datatypes.py`（SWE-bench 路径, RL 被拒）。

### 4.3 遮挡结论

- 工具调用参数（path/command/old_string/new_string）全部在 mask=1 段——
  **路径格式是被强化的学习目标**，因此必须保证其与 RL 环境一致（本次修复）
- tool 观察输出在 context（mask=0）——模型从上下文学习路径习惯，但不受梯度强化
- 训练数据（v2 实验）与 RL 环境的路径语义现在一致：`/workspace` 前缀
  归一化在 Mac server 5/5 验证通过

## 5. 训练启动（参考 formal_e1_lr2e4 上次配置）

- 脚本：`scripts/run_sft_v2.sh`（nohup + torchrun 4 卡, 参数与 2026-08-09
  formal_e1_lr2e4 完全一致: lora rank16/alpha32, batch16 micro1, max_length
  8192, lr 2e-4 + warmup 0.03, save_freq 68, 1 epoch ~138 步）
- 关键 env：`PYTHONPATH=src`（sweforge 包 import）、`HF_ENDPOINT=hf-mirror.com`、
  `HF_HOME=/root/autodl-tmp/huggingface`（缺 PYTHONPATH 会
  `ModuleNotFoundError: No module named 'sweforge'`——第一次启动失败的教训）
- 数据：`experiments/sft_qwen3_4b_lora_r16_e1_v2_20260811/data/train.jsonl`（2918 条）
- 产物：`.../formal_e1_lr2e4/checkpoints/`（step_68/step_136, adapter 取 step_136）
- 训练时长：~27s/步 x 138 ≈ 63 分钟（4 卡, 17-19GB/卡）

## 6. 后续

1. step_136 导出 adapter（`export_lora`）+ vLLM 验证
2. v15 GRPO 用干净 adapter 启动（预期：模型生成 /workspace 路径 ->
   Mac 补丁归一化 -> str_replace 成功 -> patch>0 -> reward 分化 -> 学习信号）
3. 文档化的污染调查可复用于未来数据源验收（`/testbed` 词表检查）
